<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Properties>
        <Property name="LOG_EXCEPTION_CONVERSION_WORD">%xwEx</Property>
        <Property name="LOG_PATTERN_LEVEL">%-5level [crypto-app,%X{traceId},%X{spanId}]</Property>
        <Property name="LOG_DATEFORMAT_PATTERN">yyyy-MM-dd HH:mm:ss.SSS</Property>
        <Property name="LOG_PATTERN">%clr{%d{${sys:LOG_DATEFORMAT_PATTERN}}}{faint} %clr{${sys:LOG_PATTERN_LEVEL}} %clr{%pid}{magenta} %clr{---}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c{1.}}{cyan} %clr{:}{faint} %m%n${sys:LOG_EXCEPTION_CONVERSION_WORD}</Property>
    </Properties>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${sys:LOG_PATTERN}"/>
        </Console>
        <Console name="Console_JSON" target="SYSTEM_OUT" follow="true">
           <JsonLayout complete="false" compact="false">
               <KeyValuePair key="service" value="crypto-app"/>
               <KeyValuePair key="traceId" value="$${ctx:traceId}"/>
               <KeyValuePair key="spanId" value="$${ctx:spanId}"/>
           </JsonLayout>
        </Console>
        <!-- config fichier de log -->
        <File name="file" fileName="logs/logs.log">
            <!-- format JSON des logs dans le fichier -->
            <JsonLayout complete="false" compact="false">
                <KeyValuePair key="service" value="crypto-app"/>
            </JsonLayout>
        </File>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console" /> <!-- Format texte par défaut dans la console -->
            <!-- <AppenderRef ref="Console_JSON" /> --> <!-- Décommentez pour JSON dans la console -->
            <AppenderRef ref="file" /> <!-- Config fichier de log -->
        </Root>

        <Logger name="sn.ucad.tdsi.crypto.controller" level="debug" additivity="false">
             <AppenderRef ref="Console" />
             <AppenderRef ref="file" />
        </Logger>

        <Logger name="sn.ucad.tdsi.crypto.service" level="debug" additivity="false">
             <AppenderRef ref="Console" />
             <AppenderRef ref="file" />
        </Logger>

    </Loggers>

</Configuration>
